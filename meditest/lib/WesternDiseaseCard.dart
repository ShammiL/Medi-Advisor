import "package:flutter/material.dart";

class WesternDiseaseCard extends StatelessWidget {
  @override
  final String name_;
  const WesternDiseaseCard(this.name_);
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}